package array;

public class ArrayDocs {
	/*
	 *  ----------------- Array --------------------
	 *   # collection of homogeneous data.
	 *   # collection/group of multiple values of same data type
	 *     with fixed size.
	 *     
	 *   # array contains duplicate data also.
	 *   # data manage in the form of indices(index starts from 0)
	 *   # random access
	 *   # contiguous memory allocation.
	 *   # array is an object in java.
	 *   
	 *   syntax :
	 *   
	 *       data_type    array_name[]  = new  data_type[size];
	 *       
	 *       e.g
	 *       
	 *         float    height[]  = new  float[5000];
	 *         String[]   country  = new  String[20];
	 *         char    []vs    = new  char[5];
	 *         
	 *        // Employee  emps[] = new Employee[50];
	 */

}
